开发：`npm run dev`（electron-vite dev）；构建：`npm run build`；打包：`npm run package`（先 build 再 electron-builder）。Agent 凭据通过环境变量注入：`KCODER_API_KEY`/`DEEPSEEK_API_KEY`、`KCODER_BASE_URL`、`KCODER_MODEL`。打包前需确保上游 `../engine` 目录存在，否则 `extraResources` 会失败。