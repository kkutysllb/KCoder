- 对外只暴露纯函数（如 `apply*`、`compact*`、`normalize*`），不持有可变状态，所有配置通过参数传入并在入口处用 `DEFAULT_*_CONFIG` 合并归一化。
- 对 `TurnItem` 的多态处理统一采用 `switch (item.kind)` 分支，每个 case 返回浅拷贝的新对象，未覆盖的 kind 直接透传原值。
- 文本裁剪一律使用 head/tail 拼接中间标记的方式（如 `[truncated for model input budget]`、`[token economy: showing first ...]`），保留上下文语义而非截断至零。
- 涉及正则变换的文本处理先用 `withProtectedSegments` 提取受保护片段（代码块、URL、路径、常量名等），再对占位符文本执行压缩，最后还原原始片段，避免破坏关键标识。
- 预算类常量集中声明在文件顶部（`CHARS_PER_TOKEN`、`CLIP_SAFETY_RATIO`、`MAX_COMMAND_LINES` 等），并以 `*_RE` / `MAX_*` 命名约定区分正则与数值阈值。