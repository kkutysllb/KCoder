TypeScript ESM 包（`"type": "module"`，`exports.*.js` 双导出），使用 `zod` 风格的 `RuntimeEventSchema.parse` 做事件契约校验，通过 `Intl.DateTimeFormat` 处理时区日期，通过 `node:crypto.createHash('sha256')` 生成 item 规范摘要用于 `recordOnce` 去重。