- 对外暴露的均为纯函数，无副作用、无外部状态，输入为只读对象，返回值不可变。
- 文本匹配前统一执行 NFKC 规范化、转小写、用 `/[\s\p{P}\p{S}]+/gu` 去除空白与标点后再做子串匹配，避免大小写/全半角/符号差异导致的漏判。
- 多语言关键词采用集中数组 + `includesAny` 工具函数批量匹配，新增词条只需追加到候选列表而非修改控制流。
- 分类器使用短路优先级的 if-chain 结构，先拦截协议错误与安全拒绝等硬边界，再基于启发式规则降级到 final_text。