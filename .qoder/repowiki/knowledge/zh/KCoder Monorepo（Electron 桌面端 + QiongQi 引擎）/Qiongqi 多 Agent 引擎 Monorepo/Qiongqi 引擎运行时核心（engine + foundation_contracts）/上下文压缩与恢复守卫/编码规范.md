- 所有对外入口采用纯函数或无副作用方法，输入通过显式 options 对象传入，避免隐式状态共享。
- 文本匹配使用正则表达式配合 `.replace(/\s+/g, '').toLowerCase()` 做归一化后再判断，以容忍空白差异。
- 长文本统一通过 `clipText` 截断并追加 `...` 省略号，防止摘要超出预算。
- 可恢复性判断优先检查 `compaction` 摘要中的固定关键字（如 `Task resumption state:`、`Active objective:`），而非解析结构化字段。