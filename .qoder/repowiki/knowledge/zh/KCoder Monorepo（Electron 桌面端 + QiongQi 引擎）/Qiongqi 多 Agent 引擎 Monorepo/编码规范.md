- 所有子包统一使用 ESM (`"type": "module"`) 与 Node ≥20 语义导入导出。
- 跨包边界一律通过 foundation/contracts 中的 interface/event 类型声明，禁止直接引用实现包。
- 每个包遵循 src/dist 分离，dist/index.js 作为单一入口，由 build.mjs 强制校验产物存在。
- 测试文件集中放在根 tests/ 下，按被测包名前缀命名（如 runtime-kernel.test.ts），由根 vitest.config.ts 统一发现。