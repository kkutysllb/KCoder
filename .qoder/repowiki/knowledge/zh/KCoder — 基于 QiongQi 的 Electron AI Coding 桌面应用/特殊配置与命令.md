根脚本 `pnpm dev` 启动 Electron 开发模式；`pnpm build:engine` 在 engine 子目录执行 `pnpm -r run build` 构建 vendored 引擎；`pnpm rebuild` 或 postinstall 钩子会运行 `electron-rebuild -f -w better-sqlite3` 编译原生模块；引擎需 Node ≥20，首次安装会自动触发原生重建。