- 对可恢复的工具分发错误统一走 `isRecoverableToolDispatchError` 判断，并在 catch 中产出 `code: 'tool_dispatch_rejected'` 的 `tool_result` item 而非抛出异常。
- 所有跨实例序列化边界都围绕 `idempotencyKey` 展开：`ToolRuntimeV3` 用它做 in-flight 去重与 effect 查找，`EffectCommitCoordinator` 用它关联 prepared intent 与 stored result。
- 对外部副作用（审批、用户输入、事件记录）一律通过构造时注入的 port 接口（`approvalGate`、`userInputGate`、`events.record`）访问，不直接依赖具体实现。
- 运行期状态变更采用 `runtimeStateSink` 回调 + `mergeRuntimeEffectState` 合并策略，保证并发批次写回后 committed/pending effects 按 key 稳定合并。