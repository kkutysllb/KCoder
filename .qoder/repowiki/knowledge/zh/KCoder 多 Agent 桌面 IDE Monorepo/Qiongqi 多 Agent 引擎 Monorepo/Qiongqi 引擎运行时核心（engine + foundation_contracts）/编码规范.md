- 跨包数据交换一律使用 foundation/contracts 中定义的 TypeScript 类型与 Zod Schema，禁止在子包间直接传递裸对象。
- 每个子包遵循 src/index.ts 单一入口导出约定，对外仅暴露最小 API 面。
- 副作用操作（持久化、网络调用、LLM 请求）集中在 services 层或 tool_runtime 中，纯计算逻辑下沉到 engine 子包。