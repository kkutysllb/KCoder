- 两个编排器共享完全相同的 `TurnOrchestratorOptions` 构造参数与 `awaitUserInput` 闭包模式，仅外层 loop 不同，step 逻辑统一走 `runOrchestratorStep` 纯函数。
- 对外部可选依赖一律通过 `...(opts.xxx ? { xxx: opts.xxx } : {})` 展开方式注入，保持构造函数签名稳定且向后兼容。
- 错误路径统一通过 `events.record({ kind: 'error', code, message })` + `turns.applyItem(makeErrorItem(...))` 双写，保证事件流与持久化项一致。
- turn 级资源清理集中在 `finally` 块中执行（finishGoalElapsedTimer、clearTurnAutoRoute、cleanupTurn、serializer.delete），避免异常泄漏。