顶层构建必须通过 `node engine/scripts/build.mjs [--clean]` 执行，该脚本按 L1→L8 固定顺序逐个 `pnpm run build` 各包，以处理 SCC 循环类型引用；端到端脚本如 `verify-crash-recovery.mjs` 要求设置 `DEEPSEEK_API_KEY`/`DEEPSEEK_BASE_URL`/`DEEPSEEK_MODEL` 环境变量。