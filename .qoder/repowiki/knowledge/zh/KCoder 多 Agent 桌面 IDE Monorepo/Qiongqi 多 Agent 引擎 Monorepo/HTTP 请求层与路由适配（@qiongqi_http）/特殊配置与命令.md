包名 `@qiongqi/http`，工作区内部通过 `workspace:*` 引用其他 engine packages；构建命令 `pnpm build` 执行 `tsc -p tsconfig.build.json`，类型检查 `pnpm typecheck` 执行 `tsc --noEmit -p tsconfig.json`。CORS 白名单通过环境变量 `CORS_ORIGINS` 或 `GATEWAY_CORS_ORIGINS`（逗号分隔，`*` 表示全放行）配置。