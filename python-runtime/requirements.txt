# KCoder QiLin sidecar 运行时依赖
# 引擎本体：从仓库内 vendor/qilin 源码可编辑安装（开发改引擎即时生效）
# 路径相对本文件所在目录 python-runtime/，即 <repo>/vendor/qilin
# vendored 版本见 vendor/qilin/VENDOR_VERSION；跟进上游用 git subtree pull
#
# ⚠️ QiLin v2.0.0 起 pyproject 把大量集成依赖（langchain>=1.3、kubernetes、
# e2b-code-interpreter、duckdb、exa-py、tavily-python、firecrawl-py、
# markitdown[all] 等）声明为 base dependencies，安装 -e ../vendor/qilin 时会
# 一并拉取。这些是"与上游保持一致"的代价；KCoder 运行时大多不直接使用，
# 如需瘦身可单独评估（裁剪需改 vendor/qilin/pyproject.toml，谨慎）。
#
# ⚠️ 切勿安装 qilin[gateway] extra：它要求 bcrypt>=5.0.0，与下方 passlib 路径
# （bcrypt<4.1）互斥。KCoder 用自己的 kcoder_gateway/auth，引擎核心（qilin/）
# 不 import bcrypt，故不装 extra 即可避开冲突。
-e ../vendor/qilin
# LangGraph CLI（提供 langgraph dev 命令）
langgraph-cli>=0.4.24
# Gateway 自身依赖（FastAPI + uvicorn 用于 Phase 2 的 /v1/* 翻译层）
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
# httpx>=0.28.0：v2.0.0 引擎 pyproject 要求（v1.0.0 已是此下限）
httpx>=0.28.0
# Phase 6: 真实认证（bcrypt 密码哈希 + JWT 令牌）
# 注意：passlib 1.7.4 与 bcrypt >=4.1 不兼容（__about__ 属性已移除），
# 必须把 bcrypt 锁定在 4.0.x，否则 hash() 时抛 AttributeError。
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<4.1.0
PyJWT>=2.8.0
email-validator>=2.0.0
# MCP 适配器（QiLin 通过它连接 worktree-overlay 等 MCP server）
# 注意：mcp>=2.0.0 API 不兼容，必须 pin <2.0.0；
# 下限 1.29.0 对齐 v2.0.0 引擎要求。
langchain-mcp-adapters>=0.3.0
mcp>=1.29.0,<2.0.0
