diff --git a/lib/index.js b/lib/index.js
index 03d4eb23194915649e9c34d03513beb616215c88..5f1c709c32279f40777035cae43b6454f5bd0d5b 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -1060,10 +1060,6 @@ function createTimelineState() {
     systemTokens: 0,
     toolsTokens: 0,
     toolList: [],
-    model: void 0,
-    provider: void 0,
-    lastModel: void 0,
-    contextWindow: void 0,
     requests: [],
     events: [],
     archived: [],
@@ -1123,7 +1119,7 @@ function applySurface(st, ev, type, data, message) {
     if (utext !== "") node.text = utext;
   }
   const shadowedSeqs = st.pendingShadowedSeqs;
-  st.pendingShadowedSeqs = void 0;
+  delete st.pendingShadowedSeqs;
   const op = ev.surfaceOp;
   if (op !== null && typeof op === "object" && op.op === "replace") {
     if (Array.isArray(shadowedSeqs) && shadowedSeqs.length > 0) {
@@ -1243,8 +1239,8 @@ function applyTimeline(state, event, bounds) {
       const s = ensure();
       const total = s.systemTokens + s.toolsTokens + s.sums.user + s.sums.inject + s.sums.assistant + s.sums.tool;
       const record = {
-        turn: data && typeof data.turn === "number" ? data.turn : void 0,
-        step: data && typeof data.step === "number" ? data.step : void 0,
+        ...(data && typeof data.turn === "number" ? { turn: data.turn } : {}),
+        ...(data && typeof data.step === "number" ? { step: data.step } : {}),
         time: event.time,
         seq: event.seq,
         system: s.systemTokens,
